<h1>Cats</h1>

<ul>
    <% @cats.each do |cat|%>
        <li> 
            <!-- /cats/<% cat.id %> -->
            <a href="<%= cat_url(cat) %>"> <%= cat.name %> </a> 
        </li>
    <% end %>
</ul>